import { StyleSheet } from 'react-native';

export const Styles = StyleSheet.create({
  productsContainer: {
    flex: 1,
  },
  addButtonContainer: {
    flex: 1,
    justifyContent: 'flex-end',
    alignItems: 'flex-end',
  },
});
