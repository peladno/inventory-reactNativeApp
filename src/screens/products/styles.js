import { StyleSheet } from 'react-native';

export const Styles = StyleSheet.create({
  productsContainer: {
    flex: 1,
  },
  addButtonContainer: {
    position: 'absolute',
    bottom: 10,
    right: -10,
  },
});
